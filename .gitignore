.pio
.vscode
.venv

# Emulator build output (emulator/build.sh)
emulator/dist

# The app's dependencies (app/package.json)
node_modules

# The mobile loop's runtime directory: logs, pids, the prepared
# marker, the base URL, screenshots. Nothing at the repo root.
.dev/
