<!--
Written to be read on a phone, by someone who did not watch it happen, will
not check out the branch and has no module on the desk. Lead with what changed
and why; the diff is underneath if they want it.
-->

## What changed

<!-- Grouped by issue when the branch closes several:

**#NNN — the surface**
- …
-->

## Seen, not just green

<!-- `bash scripts/checks.sh` passes — and the evidence a green tick does not
     carry. For a change to the app: screenshots, attached, at every width it
     crosses. For a change to the firmware: the test that now covers it, named.
     For a protocol change: that both halves were checked, because the
     generated protocol module is the thing that goes stale.
     A visual change with no picture is not finished. -->

## Notes

<!-- Anything deliberately left out, and why. Anything found on the way that
     is not fixed here. Anything that needs hardware to confirm — say so
     plainly rather than implying it was tried. -->

<!--
One keyword per issue, one per line. `Closes #1, #2` links the first and
silently drops the rest; a bare `#1` links without ever closing. Both leave
issues open behind a merged PR.

Closes #NNN
Closes #NNN
Part of #NNN
-->
